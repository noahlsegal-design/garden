// Your Supabase project, where check-offs are saved once the app is online. Both values come from the
// project's Connect button (or Settings > API Keys) and are safe to publish: the publishable key only works
// together with the security rules in supabase/setup.sql. Never put a secret key (sb_secret_...) here.
// Leave both empty to save check-offs on the Mac instead.
export const SUPABASE_URL = "https://ydnnwuneimhrqymkkqtu.supabase.co";
export const SUPABASE_KEY = "sb_publishable_bet6pNdkxTe_m-Q7uxXaBA_EC63bpr5";
